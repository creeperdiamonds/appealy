# Phase 0 execution log

The working record from executing `docs/superpowers/plans/2026-08-26-phase-0-reliability.md`.
Preserved because git history holds the commits but not the reasoning: the
pre-flight conflict scan, every decision taken without asking, every finding
deferred rather than fixed, and what each costs if it turns out wrong.

Rulings 24-27 are parked residuals from the final review. They are open work,
not closed decisions.

---

# SDD ledger — plan: docs/superpowers/plans/2026-08-26-phase-0-reliability.md

Spec: docs/superpowers/specs/2026-08-26-appealy-reliability-and-marketing-design.md (read, reachable)
Branch: docs/reliability-and-marketing-spec (off main, tree clean at start)

## Pre-flight conflict scan

### Cross-task: shared files and interfaces

| Tasks | Produces → Consumes | Finding |
|---|---|---|
| 1 → 8 | `describeDiscordError`, `isUnknownInteraction` from `bot/src/utils/discordError.ts` | Clean. Task 8 reads `info.status`, `.code`, `.message`, `.raw` — all four are on `DiscordErrorInfo`. |
| 2 → 4,5,6,7 | `defer(bot, interaction, opts?)`, `finish(bot, interaction, payload)` | Clean. Signatures identical at every call site in tasks 4–7. |
| 3 → 4,5,6,7 | Guard test's `MUST_DEFER` / `MUST_NOT_DEFER` arrays | Clean. Task 7 Step 3 edits the file Task 3 created; that is intended, not a collision. |
| 4 → 3 | `reviewAccept.ts` in `MUST_DEFER` | Clean. Task 4 keeps a `respond` wrapper that delegates to `finish`, so no `sendInteractionResponse` remains and the guard's negative assertion holds. |
| 6 → 3 | `formSelectStep.ts` classification | **CONFLICT** — Task 3 lists it in `MUST_DEFER`; Task 6 Step 2 says it may belong in `MUST_NOT_DEFER`. See Ruling 2. |
| 7 → 3 | `apply.ts` classification | Clean. Task 7 builds its list from what it finds rather than pre-declaring, so no contradiction. |
| 9 → 9 | `applyBanChange` exported from `banCache.ts`, consumed by `controlServer.ts` | Clean. Plan instructs matching the real map variable name rather than assuming. |
| 9 → 9 | `requestBanChange` signature | **INCONSISTENCY** — Interfaces block says `ban: PublicBan`, code block says `ban: unknown`. See Ruling 3. |
| 10 | `pricing.ts` authoritative, HTML static | Clean. Step 1 verifies the constant before any edit. |
| 11 | `service.yaml`, `bot/deno.json`, unnamed publisher file | Step 4 depends on `scripts/render-service.py` running locally. See Ruling 5. |

### Per-task self-consistency

| Task | Tests vs code / files vs later touches | Finding |
|---|---|---|
| 1 | 7 test cases traced by hand against the implementation | Clean — `null`, non-string, cause-chain and generic-message cases all resolve as asserted. |
| 2 | 4 test cases vs `defer`/`finish` | Clean — non-ephemeral asserts `{type:5,data:{}}`, which the code produces. |
| 3 | Asserts `MUST_NOT_DEFER` files contain `type: 9` | Clean — verified all three do (`panelOpen:182`, `reviewDeny:94`, `verify:50`). |
| 4 | Edits match the file read at plan time | Clean. |
| 5 | `ticketOpen` given in full; `ticketClose` given as three named edits | Clean — edits are enumerated, not "same as above". |
| 6 | Seven files, one edit shape, one flagged exception | Clean given Ruling 2. |
| 7 | Step 1 filters the list before Step 2 edits it | Clean. |
| 8 | Claims "PASS — 11 tests" | Clean — 7 (Task 1) + 4 (Task 2) = 11. |
| 9 | Five files, all named, imports specified | Clean given Ruling 3. |
| 10 | Grep in Step 5 verifies Steps 2–4 | Clean. |
| 11 | Prose-only for the publisher edit | Accepted — the file is unknown until Step 1's grep, so prescribing code would be a guess. |

### Rulings made before execution

Ruling 1: Work proceeds on branch `docs/reliability-and-marketing-spec` without a separate git worktree — this is a solo repo, the tree was clean, and a worktree would relocate the working copy the user deploys from. Cost if wrong: none to the code; the user's single checkout carries the branch instead of a parallel directory.

Ruling 2: `formSelectStep.ts` stays in `MUST_DEFER` as Task 3 writes it. Task 6's implementer reads the file and moves it to `MUST_NOT_DEFER` if it opens a modal on any path. This is deliberate plan design — a modal cannot follow a deferral, and guessing which way that file goes was the alternative. Cost if wrong: the guard test fails on that one entry and the implementer moves it; self-correcting within the task.

Ruling 3: `requestBanChange` takes `ban: PublicBan` if that type imports cleanly into `api/src` from the shared types, and `unknown` if it does not. The Interfaces block wins over the illustrative code block. Cost if wrong: a loosely-typed argument on an internal bridge function whose only caller already holds a `PublicBan`.

Ruling 4: Task 3 deliberately commits a failing test. If a reviewer flags that as a defect it is plan-mandated and I adjudicate it then — I will not pre-judge it in the reviewer's prompt. Cost if wrong: one review round spent on a finding I already expect.

Ruling 5: If `scripts/render-service.py` cannot run locally in Task 11 Step 4 (it needs eight substitution variables from the deploy environment), the implementer substitutes a grep of `deploy/service.yaml` confirming `STATUS_OUT_DIR` is present and no `_VALUE` placeholder is left unsubstituted. Cost if wrong: weaker local verification of a config change that CI re-renders and validates on deploy anyway.

## Progress

Task 1: dispatched (haiku), BASE=080b0003, brief+report in workspace
Task 1: complete (commits 080b000..7637983, review clean)
Task 1: minor (deferred): pickNumber's numeric-string coercion and asRecord's array-body path are untested branches (inherited from the brief's own code/test pairing)
Task 1: minor (deferred): the cause walk follows record.cause but not body.cause, so a cause nested under body is missed
Ruling 6: Task 6 converts pollVote.ts as planned even though the user has asked for polls to move to Discord's native feature, which would delete that handler. The polls work is not yet specced or approved, and leaving a live handler timing out in the meantime is worse than five minutes of work that a later deletion discards. Cost if wrong: one small conversion thrown away.
Task 2: implemented (commit 7f8d4ca, 4/4 passing), review dispatched (sonnet) with named risks: unspecified `as any` cast, ephemeral-flag placement
Task 2: complete (commits 7637983..7f8d4ca, review clean)
Task 2: minor (deferred): interactionResponse.ts:141 uses `data as any`; reviewer verified `as InteractionCallbackData` typechecks and is the narrower cast. Flag for the final review.
Ruling 7: The plan's Task 2 code block was defective — `InteractionEditPayload.embeds?: unknown[]` does not typecheck against Discordeno's `InteractionCallbackData`, so a cast was always going to be needed and the brief did not say so. The implementer's deviation was correct. No fix loop for a Minor; the narrowing goes to the final review. Cost if wrong: an `any` survives on one argument of one helper.
Task 3: implemented (commit 85d25f8, 3 passing / 10 failing as designed), review dispatched (sonnet) with named risks: path existence, MUST_DEFER vs MUST_NOT_DEFER classification, whether the textual assertions can pass accidentally
Ruling 8: Task 3's review raised an Important, plan-mandated finding — the guard asserts `defer(` is PRESENT but not that it comes FIRST, so a handler calling defer() as its last statement would pass while still blowing the 3-second window. I rule the finding stands over the plan text. Verified all ten MUST_DEFER files define their exported handler before their first `await`, so an ordering assertion ("the first await in the file must be `await defer(`") is safe and cannot be shadowed by a helper function. Entering the fix loop rather than deferring, because the guard is the only automated protection Tasks 4-7 have and a guard that can go green over the live bug is worse than no guard. Cost if wrong: a false failure on a file whose structure I mis-surveyed, which the implementer would report rather than silently work around.
Task 3: fix round 1/5 (ordering assertion added, shape unchanged 3 pass / 10 fail; commits 85d25f8..c2d5607), scoped re-review dispatched (haiku)
Task 3: complete (commits 7f8d4ca..c2d5607, 1 fix round, review clean)
Task 3: minor (deferred): indexOf("await ") can in principle match inside a comment or string literal; safe for these ten files (verified handler-before-first-await) but the guard would need a real parse to be airtight for arbitrary future files.
Task 4: implemented (commit d8fc511, deno check clean, guard reviewAccept passes / 9 others fail as expected), review dispatched (sonnet) with named risks: abandoned-interaction paths after defer, deferral placement vs silent guards, type:4 envelope unwrapping, residual direct sendInteractionResponse
Ruling 9: Task 4's review found a Critical the plan never anticipated — deferring converts a visible failure into a permanent hang. An exception after defer() propagates to the router's catch (interactionCreate.ts:172-191), whose fallback sends a fresh type:4 that Discord rejects as already-acknowledged; the rejection is swallowed by a nested empty catch, so the user's "thinking..." never resolves. Verified the pre-change behaviour was strictly better: undeferred, that same fallback worked.
  I rule the fix belongs in the ROUTER, not per-handler as the reviewer suggested. Try editOriginalInteractionResponse first (reaches a deferred interaction), fall back to sendInteractionResponse (reaches an unacknowledged one). One change covers all 20+ handlers this plan converts plus every future one; twenty copies of a try/catch is precisely the "handler 33 forgets" failure the guard test exists to prevent.
  Cost if wrong: the router's error path edits a successful response into an error message in the narrow case where a handler responds and then throws — which is arguably correct anyway, since the operation did fail.
Ruling 10: This fix touches bot/src/events/interactionCreate.ts, which Task 8 also modifies. Task 8's dispatch must carry a pointer to this change so it amends rather than reverts it.
Task 4: fix round 1/5 (router edit-then-send fallback; guard 4 pass / 9 fail, reviewAccept still green; commits d8fc511..f8c172b), scoped re-review dispatched (sonnet), asked to challenge the router-level ruling on its merits
Task 4: complete (commits c2d5607..f8c172b, 1 fix round, Critical addressed)
Task 4: minor (deferred): router's edit-first fallback (interactionCreate.ts:193-205) will overwrite a genuine success message with "Something went wrong" if a handler responds and THEN throws. Dormant — every converted handler's terminal response is its last statement.
Ruling 11: I accept that gap rather than fixing it. The only precise discriminator between "deferred but unfinished" and "finished with real content" is process-global state tracking live deferrals, which adds a leak failure mode on a hot path to guard a case no handler currently produces. Mitigation instead: Tasks 5-7 dispatches and reviews carry the invariant "a converted handler's terminal response must be its last statement". Cost if wrong: a future handler doing post-response work reports a successful action as failed — visible and recoverable, and strictly less bad than the hang this round fixed.
Task 5: implemented DONE_WITH_CONCERNS (commit 42f2f71, guard 6 pass / 7 fail, both ticket cases green, no regressions). Two declared deviations under review: (a) ticketClose reordered closeTicket() before finish() and changed user text "Closing this ticket..." -> "Ticket closed."; (b) also converted handleTicketRateButton and handleTicketClaimButton, unnamed in the brief, on the reasoning that the guard forbids any sendInteractionResponse in the file.
Task 5: complete (commits f8c172b..42f2f71, review clean, both deviations verified correct)
Task 5: resolved the reviewer's warning-item — the "escalate rather than reorder" clause it could not find came from my dispatch prompt, not the brief. Not a gap.
Task 5: minor (deferred): ticketClose.ts:60-65 sends "Ticket closed." without branching on closeTicket() returning null, which signals a lost double-close race. Final state is still true; the message just implies this click did it.
Task 5: minor (deferred): task-5-report.md:91 summary line undercounts response paths against its own breakdown. Cosmetic, report only.
Lesson carried forward: converting a shared respond() helper to finish() forces EVERY caller of that helper to defer, or the edit 404s against an unacknowledged token. Tasks 6 and 7 must check for shared helpers before scoping a file's conversion.
Task 6: implemented DONE_WITH_CONCERNS (commit ea793a8, guard 13/13, deno check clean). Ruling 2 RESOLVED by evidence: formSelectStep.ts DOES reach a modal, indirectly via showApplicationModal() in panelOpen.ts, so it moved to MUST_NOT_DEFER unconverted. Three declared items under review: (a) a MODAL_MARKER override added to the guard because formSelectStep never writes "type: 9" itself; (b) formSubmit.ts has role automation + review-embed posting AFTER its response, violating the terminal-response-last invariant, left as-is with a comment per instruction; (c) deno.lock picked up incidental std-lib hashes.
Note: item (b) makes Ruling 11's "dormant" gap LIVE. If formSubmit's post-response work throws, the router overwrites the applicant's "submitted" confirmation with "Something went wrong" on a submission that actually succeeded. Awaiting the reviewer's severity call before ruling on the fix; my current preference is to wrap that post-response work in its own try/catch so it can never reach the router, rather than reorder when the applicant is acknowledged.
Ruling 12: Task 6's reviewer called the implementer's "per the brief's instruction" citation a FABRICATED rationale. It was not. That instruction came from my dispatch prompt, which reviewers are not given. Second time this has happened (Task 5's warning-item was the same). The implementer behaved correctly; the misattribution is mine to own. Process fix applied below.
Ruling 13: Both Important findings stand and both get fixed this round.
  (a) formSubmit.ts:128-131 — four unguarded awaits after the response (findUnmanageableRoles x2, db.update, getGuild, sendTemplatedDm). Confirmed traceable, not theoretical. FIX: wrap the two post-response calls in their own try/catch at the call site so no exception can reach the router. NOT reorder — making an applicant wait on role automation and embed posting before hearing "submitted" is a worse trade than the failure it prevents, and both callees already self-log partial failures.
  (b) deferGuard MODAL_MARKER — an arbitrary per-path string with no verification the marker's target opens a modal. A future entry could claim "import" and opt out entirely. FIX: replace with a delegate map tying the override to an already-verified fact — the target file must itself be in MUST_NOT_DEFER, whose own type:9 assertion is what proves a modal is reachable.
  Cost if wrong on (a): a genuinely fatal post-submission error now goes unreported to the applicant, though it is still logged. Acceptable — the submission itself succeeded and is recorded either way.
Task 6: fix round 1/5 (formSubmit trailing work wrapped; MODAL_MARKER replaced with MODAL_DELEGATES tied to a verified MUST_NOT_DEFER entry; stale comment corrected; guard still 13/13; commits ea793a8..d14befd), scoped re-review dispatched (sonnet) and told to try to DEFEAT the new guard rather than confirm it
Task 6: fix round 1 verdict — (a) formSubmit wrap ADDRESSED, (c) stale comment ADDRESSED, (b) MODAL_DELEGATES NOT ADDRESSED. The re-reviewer defeated my own directed fix: fn:"import" satisfies src.includes(fn) for any TS file, and MUST_NOT_DEFER.includes(definedIn) is satisfiable by self-reference because the loop iterates that array. My formulation relocated the hole instead of closing it.
Ruling 14: Round 2 replaces substring checks with structural ones. The delegate's target must DECLARE the function (/function\s+<fn>\b/), must itself contain type: 9, must not be the entry's own path, and the caller must CALL it (/\b<fn>\s*\(/) rather than merely mention it. fn:"import" then fails on the declaration check, which is the specific defeat the re-reviewer demonstrated. Reading the target file directly also removes the need for definedIn to be in MUST_NOT_DEFER at all, which was the weak half of my own design. Cost if wrong: the guard rejects a legitimate future delegate whose target uses an unusual declaration form (const arrow export), which surfaces as a loud test failure rather than a silent exemption.
Task 6: minor (deferred): dmApplicationService.ts:215 calls postReviewEmbedForSubmission with no try/catch — same class as finding (a) but in the DM-application flow rather than an interaction, so the router-overwrite consequence does not apply. Flag for the final review.
Task 6: fix round 2/5 (structural delegate checks + negative-test evidence; guard 13/13; commits d14befd..d53a31e), scoped re-review dispatched (sonnet) with an explicit list of defeat attempts to try, including regex-injection via an unescaped fn interpolated into RegExp
Task 6: fix round 2 verdict — NOT ADDRESSED. Narrowed (fn:"import" is dead) but a non-contrived defeat remains: fn:"respond" paired with any MUST_NOT_DEFER target passes all four checks, because (i) every file in interactions/ declares its own private respond() helper, (ii) the call-site regex \brespond\s*\( matches "async function respond(" — the declaration, not a call, so no call is required at all, and (iii) the targets genuinely declare respond AND contain type: 9.
Ruling 15: Round 3 adds the one check that actually binds caller to target — the caller must IMPORT fn from definedIn. A file's own local respond() is not imported, so the demonstrated attack dies at that assertion. Also validating fn against /^[A-Za-z_$][\w$]*$/ before it reaches any RegExp, which closes the injection vector by construction rather than by escaping.
  Self-assessment: the deeper error is mine and predates both rounds. I specified a GENERIC extensible override mechanism for exactly ONE file. Each round I hardened the generic thing instead of asking why it was generic. The import check is decisive and cheap enough to justify one more round; if round 3 is also defeated I will delete the mechanism outright and special-case the single file, which is what YAGNI called for at the start.
  Cost if wrong: a future legitimate indirect-modal handler must import its delegate by name rather than reach it some other way, which is a constraint I am happy to impose.
Task 6: fix round 3/5 (import-binding + export + identifier validation; two negative-test transcripts; guard 13/13; commits d53a31e..44f600c), scoped re-review dispatched (sonnet). Explicitly authorised the reviewer to recommend DELETING the generic mechanism in favour of hardcoding the single case, and told it that recommendation will not be read as a failure of the round.
Task 6: fix round 3 verdict — NOT ADDRESSED. Reviewer scripted the assertion logic against real files: {fn:"grantVerifiedRole", definedIn:"verify.ts"} on verifyCaptcha.ts passes selfRef, identifier, callSite, importCheck and type9Check on genuine unmodified code, blocked ONLY by the export regex not recognising `export { grantVerifiedRole }`. Soundness rests on an incidental style choice in an unrelated file that the guard neither owns nor can pin.
Ruling 16: Deleting the generic mechanism, as I said I would if round 3 failed. Three rounds produced three structurally identical defeats — independently satisfiable checks that never causally tie fn to the type:9 emission. The flaw is the generic design, not any round's implementation: a {fn, definedIn} map is a pair of values a future entry chooses, so every check written against them is satisfiable by coincidence. There is exactly ONE real indirect case. A hardcoded assertion has no caller-controllable input and therefore no attack surface at all.
  This is round 4, so per the skill it goes to a FRESH implementer on a more capable model. Cost if wrong: a second indirect-modal handler someday gets a second hardcoded block instead of a map entry, which is the outcome I now want anyway.
Task 6: fix round 4/5 (MODAL_DELEGATES deleted, single case hardcoded, export check retained as a fixed literal; 13/13; mutation-tested by appending defer( to formSelectStep and confirming red; commits 44f600c..df71c22), scoped re-review dispatched (sonnet). Warned the reviewer against manufacturing a fourth finding out of momentum, and equally against softening a real one because four rounds have been spent here.
Task 6: minor (deferred): formSelectStep.ts:25 comment still cross-references "this file's entry in deferGuard.test.ts's MUST_NOT_DEFER list", stale after round 4 moved it to a dedicated test. Substance correct, cross-reference wrong. Left because round 4 was constrained to touch no production code; one-line fix for the final review wave.
Task 6: complete (commits 42f2f71..df71c22, 4 fix rounds, finding (b) closed by deleting the mechanism)
Task 6: minor (deferred): the dedicated formSelectStep test dropped round 3's import-binding tie. Its two assertions are independently satisfiable — formSelectStep contains "showApplicationModal(" and, separately, panelOpen exports it and holds type: 9. A deliberate same-named shadow would pass. Not Important because the unconditional !defer( assertion is intact and still catches the real regression. A one-line hardcoded `from "../buttons/panelOpen.ts"` check would restore the tie with no mechanism.
Task 6: minor (deferred): reviewer suggests a hardcoded ordering assertion (showApplicationModal( appearing after the type: 7 response) to partially prove the call sits on the "all answered" path. Fixed literals only, same risk profile as the rest of the dedicated test.
Ruling 17: Accepting both as deferred Minors rather than opening round 5. The skill is explicit that Minors never enter the fix loop, the reviewer verified neither reopens the defer bypass, and four rounds is already the most expensive task in this plan. Cost if wrong: the guard proves slightly less about its own premise than it could, while still catching the regression it exists for.
Task 7: implemented (commit 59b7f1b, guard 26/26 = 13 baseline + 10 generic MUST_DEFER + 2 autocomplete dedicated + 1 apply dedicated; 12 of 13 converted). apply.ts excluded: reaches a modal via runApplicationFlow -> proceedToQuestions -> showApplicationModal. Review dispatched on OPUS rather than sonnet: largest diff in the plan (54KB) and it introduces a new exception class (autocomplete-bearing files keeping a direct sendInteractionResponse), which is exactly the shape that cost four fix rounds in Task 6. Named risk 1 asks whether the dedicated tests still assert the COMMAND handler defers, or whether they exempt the whole file.
Task 7: review APPROVED with one Important (defer-without-finish fall-through in antiRaid.ts:70 and giveaway.ts:62 — unrecognised subcommand defers and never finishes, a 15-minute hang; unreachable today only because Discord validates subcommand names against the registered definition, which is a separate object from the handler).
Ruling 18: The fix round also converts importAppy.ts and importAppealy.ts, which the reviewer found out of scope. Both await isGuildOwner() — a REST call — before their first response, the exact defect this plan exists to eliminate. They were missing from my candidate list, so this is a plan defect rather than scope creep: shipping Phase 0 with two known-unconverted handlers would make the guard's 26/26 a false statement about the codebase. Guard count moves 26 -> 28. Cost if wrong: two extra conversions in a fix round rather than their own task, reviewed as one unit.
Task 7: minor (deferred): deferGuard autocomplete test uses callIndex > autocompleteIndex, a text-position proxy rather than a scope check; the type: 8 assertion covers the realistic case, so defence-in-depth only.
Task 7: minor (deferred): the apply.ts chain assertions are file-scoped, not call-scoped — refactoring panelOpen.ts so runApplicationFlow no longer reaches a modal would leave the test green. Consistent with the formSelectStep block's granularity, but the block's comment overclaims that it checks "every link in that chain".
Task 7: minor (deferred): apply.ts's respond() means "send the first response" while the same name in twelve sibling files means "edit the deferred one". Needs a one-line comment so nobody "fixes" the inconsistency.
Task 7: minor (deferred): seven defer comments end with an identical boilerplate sentence the helper's own header already states.
Task 7: minor (deferred): task-7-report.md:80 says 17 files / 5 untouched; the directory holds 18 and 6 were untouched.
PLAN-LEVEL GAP (for the final review, not fixable by deferring): apply.ts cannot defer because it opens a modal, but its pre-checks are NOT cache-backed — apply.ts:42 does a form lookup, then runApplicationFlow does a second (panelOpen.ts:51) plus checkGate's five-plus queries (panelOpen.ts:211-249) before the modal appears. interactionResponse.ts:16-18 requires modal handlers to keep pre-checks cache-backed; this chain violates that. /apply may still blow the three-second window with no deferral available as a remedy. This is the one handler Phase 0 does not fix.
Task 7: fix round 1/5 (fallbacks added to antiRaid + giveaway; importAppy -> autocomplete loop, importAppealy -> generic MUST_DEFER with its respond() helper relocated below execute(); guard 26 -> 28; commits 59b7f1b..16d8f2e), scoped re-review dispatched (sonnet). Asked specifically whether the relocation reveals a FALSE-POSITIVE CLASS in my guard: the ordering assertion indexOf("await defer(") === indexOf("await ") fails for a correct handler whenever a helper containing an await is declared above it, and the only remedy is moving code. If so the guard imposes layout on correct code, which is a design cost I should own rather than discover later.
Task 7: complete (commits df71c22..16d8f2e, 1 fix round, review clean, guard 28/28)
Task 7: GUARD DESIGN WART, confirmed and characterised (for the final review): the ordering assertion tests "is defer() the first await TEXTUALLY in the file", not "is defer() the first await the handler EXECUTES". Those coincide only because of a hand-maintained convention — helpers declared after handlers — which nothing enforces. A correct handler fails whenever a helper containing an await sits above it, and the only remedy the guard permits is physically moving code. Cost is a forced layout convention, not correctness. Verified behaviourally safe: async function declarations hoist fully.
Task 7: minor (deferred): importAppy.ts:122-134 calls finish() then logger.info inside the same try. If logger.info ever threw, the catch would call finish() a second time and overwrite a successful response. Pre-existing statement order carried through the conversion unchanged; logger.info is synchronous and non-throwing in practice.
Reviewer independently recounted coverage rather than trusting the report: 15 of 18 command files guarded, the other three (dashboard.ts, index.ts, ping.ts) correctly needing none, 28 tests total.
Task 8: implemented (commit 717a57f, utils 11/11, guard 28/28, type-check clean; implementer claims the edit-then-send fallback survived byte-for-byte). Review dispatched (sonnet). Named risk 2 asks whether each new log line carries MORE information than before rather than less — the old interactionCreate line emitted a full err.stack, and trading a stack trace for a status code could be a net loss for debugging. Told the reviewer to judge per site whether someone debugging the 2026-08-23 ticket failures would now be better or worse off.
Task 8: review NEEDS FIXES. Edit-then-send fallback verified intact (diff-hunk arithmetic AND live-file read). Sites 1 and 2 are clean net gains. Site 3 is an Important regression: interactionCreate.ts:191-195 drops err.stack on the non-10062 branch, and that catch wraps ~15 handlers doing business/DB/routing logic, so a genuine JS bug now logs a null status, a null code and a bare message with no file/line.
Ruling 19: The regression traces to MY plan text, which said "replace error: String(err) in the three log calls". Site 3 was never String(err) — it was `err instanceof Error ? err.stack : String(err)`. I described the wrong starting code and the implementer faithfully replaced something richer than I claimed was there. Fix: restore a stack field on the non-10062 branch, keeping the new structured fields alongside it. Cost if wrong: one extra field on an error log.
Task 8: minor (deferred): info.raw is JSON.stringify({status, code, message}) — a re-serialisation of three fields already logged beside it, so it adds nothing despite a name implying a fuller dump. That is a defect in Task 1's design (mine), not in Task 8's wiring. Worth making raw carry the actual body or stack, or deleting it.
Task 8: minor (deferred): the 10062 rationale is stated twice in close proximity in interactionCreate.ts with overlapping wording.
Noted well: the implementer correctly refused two stale details in my brief — the customId identifier (block-scoped to case blocks, not in scope at the outer catch) and a wrong ../../utils/ import path — rather than following them mechanically.
Task 8: fix round 1/5 (stack restored on the non-10062 branch alongside the structured fields; commits 717a57f..ce105d4), scoped re-review dispatched (haiku, small fix diff). Asked it to check the fix did not trade one regression for another by swapping structured fields back out, and to re-verify the edit-then-send fallback from the diff rather than the report claim.
Task 8: complete (commits 16d8f2e..ce105d4, 1 fix round, review clean)
Task 9: implemented (commit 2f7c92c, deno check clean, 11+28 tests pass, api npm run build exits 0). Ruling 3 RESOLVED by evidence: PublicBan imports cleanly into api/src via ../../../shared/schema/platformBans.ts, already used by banGate/ops/platformAppeals, confirmed by a clean tsc build. Review dispatched (sonnet). Named risk 1 is the fire-and-forget contract: the old transport was redis.publish under withRedis(...,0), the new one is fetch via callBot which THROWS on non-ok and rejects if the container is down. Asked for a per-scenario failure trace (bot down / restarting / 401 / 500 / timeout) and specifically whether an un-timed-out fetch can HANG, since invalidateOnWrite fires from a res.on("finish") callback.
Task 9: review NEEDS FIXES. DRY, transport selection and the PublicBan boundary all verified correct. Critical: rejections are safely swallowed but a HANG is not. invalidateGuildCache fires from res.on("finish") so it cannot stall a response; publishBanChange is awaited synchronously BEFORE the response in ops.ts:116, ops.ts:129 and platformAppeals.ts:308, and callBot sets no AbortSignal. A bot that has bound its port but is not yet servicing requests does not reject — it hangs to undici's ~300s default, exceeding Cloud Run's request timeout, so an admin sees a failed ban that actually committed.
Ruling 20: Fix at the transport, not the call sites. callBot gets a bounded AbortSignal.timeout, which protects every existing caller too — requestLockdownClear, requestPanelPublish, requestPollPublish and the rest share the same unbounded fetch and the same latent 300s stall. A default around 10s is far above what a few Discord REST calls need and far below anything a user or Cloud Run would wait through; the two fire-and-forget invalidation calls get a shorter bound since nothing reads their result. Moving the ban-change call after the response was the alternative and I rejected it: it would touch three call sites, leave the underlying unbounded fetch in place for every other caller, and fix one symptom of a transport-level defect.
  Cost if wrong: a genuinely slow but valid control-server operation is aborted at the bound and reported as failed rather than completing. Mitigated by setting the bound well above realistic work.
Task 9: fix round 1/5 (callBot bounded with an AbortSignal, shorter override on the fire-and-forget calls; commits 2f7c92c..7b755e5), scoped re-review dispatched (sonnet). Asked it to check the bound covers headers-never-arrive rather than only a slow body, that aborts are still swallowed on the fire-and-forget paths, that result-returning callers now fail sensibly rather than silently, and that any AbortController timer is cleared on success so it does not leak handles.
Task 9: complete (commits ce105d4..7b755e5, 1 fix round, Critical addressed). Bound verified to cover headers-never-arrive; AbortSignal.timeout needs no manual cleanup; Node engines >=24, running v25.9.0, well above the 17.3 floor. Ban-change worst case is now ~2s added latency on an already-committed write instead of a ~300s stall surfacing as failure.
Task 10: implemented (commit 0a419f3, constant verified 3_000, implementer's grep clean). Review dispatched (sonnet, escalated from haiku after I found more).
Investigated dist-site/ and cleared it: NOT tracked by git (0 files) and build-site.sh does `rm -rf "$OUT"` then copies from site/, so it is a local artifact that regenerates from the corrected source. Not a defect, nothing ships from it.
But it exposed a real gap in my brief: it holds the price in a <meta name="description"> and in a sentence framing "$10/year ... as $0.83 a month" — a FIFTH location class my four-site list never mentioned, and one the implementer's grep pattern ('\$10/year|"10.00"') would miss if phrased as "$10 a year" or as a monthly equivalent. Meta descriptions are what search results and link previews render. Told the reviewer to sweep site/ itself for any phrasing, and to check the monthly arithmetic ($30/year is $2.50/month, not $0.83).
Task 10: complete (commits 7b755e5..0a419f3, review clean). Implementer independently found and fixed the two locations my brief missed (meta description, $0.83/month illustration) BEFORE running the narrow grep, so the grep's blind spot never mattered. Reviewer parsed both JSON-LD blocks with JSON.parse itself rather than trusting the claim, and verified the dedicatedRunner comment's "eight-fold gap" was correctly REMOVED rather than left stale ($84/$30 is 2.8x, not 8.4x).
Task 10: minor (deferred): the new paragraph at site/pricing.html:324-326 does not match the file's indentation convention. Inherited verbatim from my brief's snippet.
ACTION FOR THE USER (not a code defect): dist-site/ is a stale untracked build artifact still showing $10 in five places. Run `sh scripts/build-site.sh` before any static-host deploy, or the live marketing site keeps advertising the old price.
Task 11: implemented (commit e1b820b, deno check clean, 11+28 pass). Opt-in branch taken, NOT the brief's default-on /tmp/status branch. Ruling 5's render-service.py fallback never needed — service.yaml was not touched at all.
Ruling 21: My plan's Task 11 was half wrong and I flagged it at dispatch rather than letting it ship. Pointing STATUS_OUT_DIR at /tmp/status would have written status.json into the bot container's own filesystem, which the web/nginx container cannot read, since Cloud Run containers in one instance do not share a filesystem and service.yaml declares no volumes. That is not a fix — it is the same uselessness plus a write permission and a file implying status publishing works. Evidence the implementer gathered: zero volume blocks in service.yaml, and status/README.md documents a shared-volume fix not wired up in docker-compose.yml either.
FOLLOW-UP FOR THE USER (out of Phase 0 scope): the status page is non-functional in EVERY environment, not just production. Nothing writes a status.json anywhere anything can read. Making it work needs the shared volume its own README describes.
Task 11: review APPROVED with one Important — the anti-regression comment at statusPublisher.ts:101-103 asserts the shared-volume wiring "is real for the docker-compose deployment", which is false and which the implementer's own report contradicts. Reviewer independently confirmed docker-compose.yml has zero status references. Fitting failure mode for a task about the system not asserting untrue things about itself; a wrong "why" comment is worse than none because it stops the reader checking. Fix round 1 dispatched.
Reviewer independently re-derived the whole branch decision rather than trusting it: read service.yaml in full (zero volumes), nginx.conf:101-104 (try_files over baked-in statics, no /status/status.json route — that exists only as an EXAMPLE inside status/README.md), docker-compose.yml (no status lines, only pg_data), status/index.html:193 (client-side fetch of a file nothing serves).
Task 11: minor (deferred): no unit test for statusPublisher's three branches (unset / writable / unwritable). None existed before; cheap to add and would stop someone reintroducing a default OUT_DIR fallback.
Task 11: complete (commits 0a419f3..bfd8888, 1 fix round, review clean)

=== ALL 11 TASKS COMPLETE. Branch: 24 commits, merge-base 0a95dad. Final whole-branch review next (opus). ===

=== FINAL WHOLE-BRANCH REVIEW (opus, 24 commits, 5370-line diff read in 4 passes) ===
Verdict: Ready to merge WITH FIXES. No regression introduced by the branch. One fix wave dispatched.
CRITICAL 1: verify.ts:29-43 left unconverted — db lookup + getGuild + getMember + addRole + removeRole + insert (~1.2-1.5s sequential REST) before responding, on the NEW-MEMBER ONBOARDING path, with "button" the default method. Only the captcha branch needs the modal exemption; the button branch could always have deferred. Worse: deferGuard asserts !src.includes("defer(") for verify.ts, so the branch INSTITUTIONALISED the bug — a future fix fails the suite.
IMPORTANT 2: site/pricing.html:338 "over a third" was true at $10/yr (39%) and is false at $30/yr (~15%). Carried through Task 10 unchanged. Bad on a page whose own header promises checkable arithmetic.
IMPORTANT 3: router fallback should try send BEFORE edit — the never-acknowledged handlers are exactly those with no deferral to buy time, and edit-first burns ~300ms on them.
IMPORTANT 4: NOTHING in CI runs deno test. Five workflows, all deploy/secret-sync. Four fix rounds hardened the guard against a hostile author; zero made it run.
IMPORTANT 5: guildResources.ts:23,35 fetch the control server directly, NOT via callBot, so Ruling 20's "protects every existing caller" is overstated — those two keep the ~300s stall, on interactive dashboard dropdowns. overview.ts:73-90 already does it right with a 2s AbortController.
Reviewer also sharpened the guard wart: the assertion is PER-FILE, not per-handler, so in a multi-handler file it constrains only the textually first handler. ticketClose.ts exports three; the other two could regress green. That is a false NEGATIVE, the dangerous direction, and not the wart I recorded.
And corrected my apply.ts escalation: handlePanelOpenButton delegates to the SAME runApplicationFlow and is the primary path, so fixing /apply alone would fix the less-used of the two. reviewDeny.ts is a third, smaller instance.
Ruling 22: Spec §0.1's "those must simply stay cheap, which they are" about modal handlers is FALSE and is why nobody measured. Correcting the spec in the fix wave rather than leaving a plan that misleads Phase 1.
Final fix wave: 16 findings, ONE dispatch (opus), 2 commits bfd8888..99c417c. Guard 28 unchanged (MUST_NOT_DEFER -1, new bespoke verify.ts block +1), utils 11, combined 39, deno check clean, api build exit 0, no-explicit-any now 0.
Fixer pushed back on three of my instructions, correctly each time:
  (a) #14 had THREE raw sites not four, and the unit test never referenced it, so the utils count does not drop.
  (b) #2 — my directed fix would have introduced a different wrong number. The clause says "to the flat fee ALONE", which is 12%, not the 15% total-fee measure its neighbours use. It rewrote the wording so figure and claim agree rather than just swapping the digit.
  (c) #4 — ran tsc under working-directory: api (no root node_modules, so root npx would fetch an unpinned compiler), and left deno lint OUT of CI because 34 pre-existing findings would land it red on run one.
It also caught unprompted that interactionResponse.ts's header claimed verify.ts "must not call defer()", which Critical 1 made false.
Ruling 23: Ratifying the deno lint omission. Adding a gate that is red on its first run trains people to ignore it, which is the same disease as the status publisher warning every boot. Recording it here instead: 34 pre-existing lint findings in bot/src/ are worth a cleanup pass, after which lint should join CI.
Scoped re-review of the fix wave dispatched (opus). No second fix wave is permitted; residuals get adjudicated and surfaced.
Fix-wave re-review: READY TO MERGE. All 16 addressed, all four deviations correct on merits, scope clean (apply.ts +6 lines all comment, no defer(), five-line change absent), no breakage. Reviewer verified the numbers independently rather than re-running: guard 28 by entry count (20 MUST_DEFER + 2 MUST_NOT_DEFER + 3 autocomplete + formSelectStep + apply + verify), utils 11 by Deno.test count, three raw sites, service.yaml:58 really is maxScale "1".
Ruling 24: PARKED — the new verify.ts guard block asserts presence, not position. Moving `await defer(` above the verificationConfigs lookup would break the captcha modal in production while the test stayed green. Real, and the same false-negative class finding 15 made the MUST_DEFER loop confess to. Parked rather than fixed because the process permits no second fix wave, and because two ordering assertions are a clean standalone change. Cost if wrong: a future edit to one file breaks its modal with a green suite.
Ruling 25: PARKED — CI runs bot/src/ only, so shared/schema/__tests__ (pricing, regexValidation) still run nowhere. Exactly what finding 4 specified, so the wave complied; the gap is in my instruction. One extra command. Cost if wrong: the pricing invariant test that caught a 19.3x preset/custom mismatch keeps not running automatically.
Ruling 26: PARKED — plan doc still says verify.ts "must never call defer()" (line 16) and lists it in MUST_NOT_DEFER (line 453), both false after Critical 1. Finding 16 named only the spec, which is my omission. A live false statement in docs/ of precisely the kind this wave cleaned up. Cost if wrong: whoever picks up Phase 1 reads a plan contradicting the code.
Ruling 27: PARKED — proxyToBot's 2s bound covers headers, not a slow body stream. Inherited verbatim from the fetchBotHealth precedent it was told to follow, so not introduced here. Cost if wrong: a dashboard dropdown can still stall on a trickling response body.

=== PHASE 0 COMPLETE: 11 tasks, 26 commits, final review clean, 4 residuals parked ===
